declare class SpinnerService {
    getClasses(variant: string, full: boolean, fullscreen: boolean, className: string): {
        base: any;
    };
}
export declare const spinnerService: SpinnerService;
export {};
