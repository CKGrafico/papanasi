declare class PillService {
    getClasses(variant: string, intent: string, disabled: boolean, className: string): {
        base: any;
    };
}
export declare const pillService: PillService;
export {};
