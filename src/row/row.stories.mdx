import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { Row } from '../../packages/react';
import { variants, Intent, intents } from '../../models';
import { getBreakpointArgTypes, getDebugArgTypes } from '../../helpers';

<Meta
  title="Layout/Row"
  component={Row}
  argTypes={{
    ...getBreakpointArgTypes('Direction in {breakpoint}', ['', 'row', 'column', 'row-reverse', 'column-reverse']),
    ...getDebugArgTypes()
  }}
/>

export const Template = (args) => <Row {...args}>Row</Row>;

# Row

Todo..

# Showcase

<Canvas>
  <Story
    name="Default"
    args={{
      debug: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" of={Row} />

## How to use this component

Todo
