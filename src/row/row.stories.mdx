import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { Row } from '../../packages/react';
import { variants, Intent, intents } from '../../models';

<Meta
  title="Layout/Row"
  component={Row}
  argTypes={{
    xxs: {
      name: 'XXS',
      description: 'Direction in XXS',
      table: {
        defaultValue: { summary: '' }
      },
      options: ['row', 'column', 'row-reverse', 'column-reverse'],
      control: { type: 'select' }
    },
    m: {
      name: 'M',
      description: 'Direction in XXS',
      table: {
        defaultValue: { summary: '' }
      },
      options: ['row', 'column', 'row-reverse', 'column-reverse'],
      control: { type: 'select' }
    },
    debug: {
      name: 'Debug',
      description: 'Show debugging styles',
      table: {
        defaultValue: { summary: 'false' }
      },
      control: {
        type: 'boolean'
      }
    }
  }}
/>

export const Template = (args) => <Row {...args}>Row</Row>;

# Row

Todo..

# Showcase

<Canvas>
  <Story
    name="Default"
    args={{
      debug: true
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" of={Row} />

## How to use this component

Todo
