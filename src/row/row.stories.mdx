import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { Row, Container } from '../../packages/react';
import { variants, Intent, intents } from '../../models';
import { getBreakpointArgTypes } from '../../helpers';
import { Customization } from '../../.storybook/components';

<Meta
  title="Layout/Row"
  component={Row}
  argTypes={{
    ...getBreakpointArgTypes('Direction in {breakpoint}', ['', 'row', 'column', 'row-reverse', 'column-reverse'])
  }}
/>

export const Template = (args) => (
  <Container>
    <Row {...args}>Row</Row>
  </Container>
);

# Row

Rows are layout elements that can contain a group of other elements using flex properties. They can be used to create a grid of elements.

The direction inside a row can be different for each breakpoint: `xs`, `s`, `m`, `l`, `xl` and `xxl` and the supported directions are: `row`, `column`, `row-reverse` and `column-reverse`.

## Showcase

<Canvas>
  <Story name="Default" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

### Customization

<Customization
  css={`
    /* Grid */
    --pa-grid-gutter: 0.5rem;
    --pa-grid-gutter-horizontal: var(--pa-grid-gutter);
    --pa-grid-gutter-vertical: var(--pa-grid-gutter);
    --pa-grid-max-width: var(--pa-breakpoint-xl);
  `}
/>

### Properties

<ArgsTable story="Default" of={Row} />

## How to use this component

### Angular

```js
import { Row } from '@papanasi/angular';
import '@papanasi/angular/dist/papanasi.css';
/* TODO */
```

### React

```js
import { Row } from '@papanasi/react';
import '@papanasi/react/dist/papanasi.css';
export default function App() {
  return (
    <Row m="column" l="row">
      Your content
    </Row>
  );
}
```

### Vue

```js
<script>
import { Row } from "@papanasi/vue";
import "@papanasi/vue/dist/papanasi.css";
export default {
  name: "App",
  components: {
    Row,
  },
};
</script>
<template>
  <Row m="column" l="row">Your Content</Row>
</template>
```

### Web Components

```js
import { Row } from '@papanasi/webcomponents';
import '@papanasi/webcomponents/dist/papanasi.css';
customElements.get('pa-row') || customElements.define('pa-row', Row);
```

```html
<pa-row m="column" l="row">Your content</pa-row>
```
