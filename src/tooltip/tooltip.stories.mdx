import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { useTooltipExtension } from '../../packages/react/src';
import { Customization, Codesandbox } from '../../.storybook/components';

<Meta
  title="Extensions/Tooltip"
  argTypes={{
    title: {
      name: 'title',
      description: 'A sample of attribute title',
      defaultValue: 'Title of my tooltip',
      table: {
        defaultValue: { summary: 'Title of my tooltip' }
      },
      control: 'text'
    }
  }}
/>

export const Template = (args) => (
  <div>
    {(() => {
      useTooltipExtension(document.body.querySelector('.docs-story'));
      return '';
    })()}
    <div style={{ 'max-height': '200px', overflow: 'scroll' }}>
      <span {...args} style={{ 'font-size': '3rem' }}>
        Hover me
      </span>
      <span style={{ 'min-height': '400px', display: 'block' }}></span>
    </div>
  </div>
);

# Tooltip

Todo

## Showcase

<Canvas>
  <Story
    name="Default"
    args={{
      title: 'Title of my tooltip'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Properties

<ArgsTable story="Default" />

## How to use this extension

### Angular

<Codesandbox
  components={[]}
  extensions={['useTooltipExtension']}
  platform="angular"
  code={`
    <span title="Example of a tooltip">Hover me</span>`}
/>

### React

<Codesandbox
  components={[]}
  extensions={['useTooltipExtension']}
  platform="react"
  code={`
  <span title="Example of a tooltip">Hover me</span>`}
/>

### Solid

<Codesandbox
  components={[]}
  extensions={['useTooltipExtension']}
  platform="solid"
  code={`
  <span title="Example of a tooltip">Hover me</span>`}
/>

### Svelte

<Codesandbox
  components={[]}
  extensions={['useTooltipExtension']}
  platform="svelte"
  code={`
  <span title="Example of a tooltip">Hover me</span>`}
/>

### Vue

<Codesandbox
  components={[]}
  extensions={['useTooltipExtension']}
  platform="vue"
  code={`
  <span title="Example of a tooltip">Hover me</span>`}
/>

### Web Components

<Codesandbox
  components={[]}
  extensions={['useTooltipExtension']}
  platform="webcomponents"
  code={`<span title="Example of a tooltip">Hover me</span>`}
/>
