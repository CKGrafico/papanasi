import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { useTooltipExtension } from '../../packages/react/src';
import { Customization, Codesandbox } from '../../.storybook/components';

<Meta
  title="Extensions/Tooltip"
  argTypes={{
    title: {
      name: 'title',
      description: 'A sample of attribute title',
      defaultValue: 'Title of my tooltip',
      table: {
        defaultValue: { summary: 'Title of my tooltip' }
      },
      control: 'text'
    }
  }}
/>

export const Template = (args) => (
  <div>
    {(() => {
      useTooltipExtension(document.body.querySelector('.docs-story'));
      return '';
    })()}
    <div style={{ maxHeight: '200px', overflow: 'scroll' }}>
      <span {...args} style={{ fontSize: '3rem' }}>
        Hover me
      </span>
      <span style={{ minHeight: '400px', display: 'block' }}></span>
    </div>
  </div>
);

# Tooltip

Todo

## Showcase

<Canvas>
  <Story
    name="Default"
    args={{
      title: 'Title of my tooltip'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Customization

<Customization showCode={false} />

### Properties

<ArgsTable story="Default" />

## How to use this extension

### Angular

<Codesandbox
  extensions={['useTooltipExtension']}
  platform="angular"
  code={`
    <span title="Example of a tooltip">Hover me</span>`}
/>

### React

<Codesandbox
  extensions={['useTooltipExtension']}
  platform="react"
  code={`
  <span title="Example of a tooltip">Hover me</span>`}
/>

### Solid

<Codesandbox
  extensions={['useTooltipExtension']}
  platform="solid"
  code={`
  <span title="Example of a tooltip">Hover me</span>`}
/>

### Svelte

<Codesandbox
  extensions={['useTooltipExtension']}
  platform="svelte"
  code={`
  <span title="Example of a tooltip">Hover me</span>`}
/>

### Vue

<Codesandbox
  extensions={['useTooltipExtension']}
  platform="vue"
  code={`
  <span title="Example of a tooltip">Hover me</span>`}
/>

### Web Components

<Codesandbox
  extensions={['useTooltipExtension']}
  platform="webcomponents"
  code={`<span title="Example of a tooltip">Hover me</span>`}
/>
