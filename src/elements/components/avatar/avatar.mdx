import { Canvas, Meta, ArgTypes } from '@storybook/addon-docs/blocks';
import { Avatar } from '../../../../packages/react/src';
import * as Stories from './avatar.stories';
import { Customization, Codesandbox } from '../../../../.storybook/components';

<Meta of={Stories} />

# Avatar

Avatars are useful to show the initials of a name or the picture of a user.
By default the color will be randomly generated from the name, but there are also 3 different variants `primary`, `secondary` and `tertiary`.
If you add a `url` will be used as image and it is also compatible with [unavatar](https://unavatar.io/)

## Showcase

<Canvas of={Stories.Showcase} />

### Customization

<Customization
  selector=".pa-avatar"
  css={`
    --pa-avatar-size: 3rem;
    --pa-avatar-padding: 0.5rem;
    --pa-avatar-radius: 100%;
    --pa-avatar-initials-weight: var(--pa-font-weight-medium);
  `}
/>

### Properties

<ArgTypes of={Avatar} />

## How to use this component

### Angular

<Codesandbox
  components={['Avatar']}
  platform="angular"
  code={`
    <span pa-avatar name="Alice Morris"></span>`}
/>

### Preact

<Codesandbox
  components={['Avatar']}
  platform="preact"
  code={`
  <Avatar name="Alice Morris" />`}
/>

### Qwik

<Codesandbox
  components={['Avatar']}
  platform="qwik"
  code={`
  <Avatar name="Alice Morris" />`}
/>

### React

<Codesandbox
  components={['Avatar']}
  platform="react"
  code={`
  <Avatar name="Alice Morris" />`}
/>

### Solid

<Codesandbox
  components={['Avatar']}
  platform="solid"
  code={`
  <Avatar name="Alice Morris" />`}
/>

### Svelte

<Codesandbox
  components={['Avatar']}
  platform="svelte"
  code={`
  <Avatar name="Alice Morris" />`}
/>

### Vue

<Codesandbox
  components={['Avatar']}
  platform="vue"
  code={`
  <Avatar name="Alice Morris" />`}
/>

### Web Components

<Codesandbox components={['Avatar']} platform="webcomponents" code={`<pa-avatar name="Alice Morris"></pa-avatar>`} />

