import { Canvas, Meta, ArgTypes } from '@storybook/addon-docs/blocks';
import { Spinner } from '../../../../packages/react/src';
import * as Stories from './spinner.stories';
import { Customization, Codesandbox } from '../../../../.storybook/components';

<Meta of={Stories} />

# Spinner

This Spinner should cover most of the scenarios where you need to show a simple loading.
By Spinner the color will be `basic`, but there are also 3 different variants `primary`, `secondary` and `tertiary`.
You can also force to be `full` or `fullscreen` spinner if you don't want it inline.

## Showcase

<Canvas of={Stories.Showcase} />

### Customization

<Customization
  selector=".pa-spinner"
  css={`
    --pa-spinner-size: 3rem;
    --pa-spinner-stroke: 4;
    --pa-spinner-offset: 200;
    --pa-spinner-duration: 4s;
  `}
/>

### Properties

<ArgTypes of={Spinner} />

## How to use this component

### Angular

<Codesandbox
  components={['Spinner']}
  platform="angular"
  code={`
    <span pa-spinner variant="primary"></span>`}
/>

### Preact

<Codesandbox
  components={['Spinner']}
  platform="preact"
  code={`
  <Spinner variant="primary" />`}
/>

### Qwik

<Codesandbox
  components={['Spinner']}
  platform="qwik"
  code={`
  <Spinner variant="primary" />`}
/>

### React

<Codesandbox
  components={['Spinner']}
  platform="react"
  code={`
  <Spinner variant="primary" />`}
/>

### Solid

<Codesandbox
  components={['Spinner']}
  platform="solid"
  code={`
  <Spinner variant="primary" />`}
/>

### Svelte

<Codesandbox
  components={['Spinner']}
  platform="svelte"
  code={`
  <Spinner variant="primary" />`}
/>

### Vue

<Codesandbox
  components={['Spinner']}
  platform="vue"
  code={`
  <Spinner variant="primary" />`}
/>

### Web Components

> Note: Currently animations are not working well in shadow root

<Codesandbox components={['Spinner']} platform="webcomponents" code={`<pa-spinner variant="primary"></pa-spinner>`} />

