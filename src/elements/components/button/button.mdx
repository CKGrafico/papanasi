import { Canvas, Meta, ArgTypes } from '@storybook/addon-docs/blocks';
import { Button } from '../../../../packages/react/src';
import * as Stories from './button.stories';
import { Customization, Codesandbox } from '../../../../.storybook/components';

<Meta of={Stories} />

# Button

Buttons allow to take actions in the app, and are used for navigation, forms, or any other task.
There are 3 different variants `primary`, `secondary` and `tertiary` or the one you decide to create, but also other 4 intents `info`, `success`, `warning` and `error`.

You can customize variants changing the variables, more info in the [variables page](/docs/start--page)

## Showcase

<Canvas of={Stories.Showcase} />

### Customization

<Customization
  selector=".pa-button"
  css={`
    --pa-color-basic-brightest: #ffffff;
    --pa-color-basic-darkest: #101116;
    --pa-color-primary-normal: #017aff;
    --pa-color-secondary-normal: #6c47ff;
    --pa-color-tertiary-normal: #f34971;
    --pa-button-radius: var(--pa-border-radius-small);
    --pa-button-border-width: var(--pa-border-width-small);
    --pa-button-font-weight: var(--pa-font-weight-medium);
    --pa-button-outline: var(--pa-outline-width);
  `}
/>

### Properties

<ArgTypes of={Button} />

## How to use this component

### Angular

<Codesandbox
  components={['Button']}
  platform="angular"
  code={`
    <span pa-button variant="primary" outline="true">Primary</span>`}
/>

### Preact

<Codesandbox
  components={['Button']}
  platform="preact"
  code={`
  <Button variant="primary" outline>
    Primary
  </Button>`}
/>

### Qwik

<Codesandbox
  components={['Button']}
  platform="qwik"
  code={`
  <Button variant="primary" outline>
    Primary
  </Button>`}
/>

### React

<Codesandbox
  components={['Button']}
  platform="react"
  code={`
  <Button variant="primary" outline>
    Primary
  </Button>`}
/>

### Solid

<Codesandbox
  components={['Button']}
  platform="solid"
  code={`
  <Button variant="primary" outline>
    Primary
  </Button>`}
/>

### Svelte

<Codesandbox
  components={['Button']}
  platform="svelte"
  code={`
  <Button variant="primary" outline>
    Primary
  </Button>`}
/>

### Vue

<Codesandbox
  components={['Button']}
  platform="vue"
  code={`
  <Button variant="primary" :outline="true">Primary</Button>`}
/>

### Web Components

<Codesandbox
  components={['Button']}
  platform="webcomponents"
  code={`<pa-button variant="primary" outline="true">Primary</pa-button>`}
/>

